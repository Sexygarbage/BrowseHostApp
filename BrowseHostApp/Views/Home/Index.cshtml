@using BrowseHostApp.Models


@section Body {

    
    
        <button class="btn btn-xs btn-primary"
                                >
                                ...
                            </button>
        <div class="panel-body">
            <table ng-controller="valuesController" class="table table-striped table-condensed">
                <thead>
                    <tr><th></th><th></th><th></th><th></th></tr>
                </thead>
                <tbody>
                    <tr ng-repeat="register in list" >
                        <td> {{register.registerName}}</td>
                        <td>{{list}}</td>
                        <td></td>
                        <td>
                            <button class="btn btn-xs btn-primary"
                                ng-click="pick(register.registerName)" >
                                Выбрать
                            </button>
                        </td>
                    </tr>
                </tbody>
            </table>

        </div>
       
    
    <a href="api/values" title="ASP.NET Web API Website">Values</a>
}